[package]
name = "vestigia-cli"
version = "0.1.0-alpha.1"
edition = "2024"

[dependencies]
clap = { version = "4.5.41", features = ["derive"] }
time.workspace = true
vestigia-core = { path = "../core" }
